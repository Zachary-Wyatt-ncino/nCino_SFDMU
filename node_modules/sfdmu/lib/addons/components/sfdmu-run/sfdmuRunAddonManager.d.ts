import "reflect-metadata";
import "es6-shim";
import { AddonManifest, Script } from '../../../modules/models';
import { Logger } from '../../../modules/components/common_components/logger';
import ScriptAddonManifestDefinition from '../../../modules/models/script_models/scriptAddonManifestDefinition';
import { ADDON_EVENTS } from '../../../modules/components/common_components/enumerations';
import AddonModule from '../common/addonModule';
import SfdmuRunAddonRuntime from './sfdmuRunAddonRuntime';
export default class SfdmuRunAddonManager {
    runtime: SfdmuRunAddonRuntime;
    script: Script;
    get logger(): Logger;
    get fullCommandName(): string;
    manifests: AddonManifest[];
    addonsMap: Map<ADDON_EVENTS, [Function, ScriptAddonManifestDefinition, Function][]>;
    addons: Map<number, AddonModule[]>;
    constructor(script: Script);
    triggerAddonModuleInitAsync(): Promise<void>;
    triggerAddonModuleMethodAsync(event: ADDON_EVENTS, objectName?: string): Promise<boolean>;
    triggerAddonModuleMethodSync(event: ADDON_EVENTS, objectName: string): void;
    triggerAddonModuleInitSync(): void;
    private _loadCoreAddonManifest;
    private _loadUserAddonManifest;
    private _setupAddonDefinition;
    private _createAddOnsMap;
}
