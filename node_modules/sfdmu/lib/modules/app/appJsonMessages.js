"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path = require("path");
const appMessagesBase_1 = require("./appMessagesBase");
class AppJsonMessages extends appMessagesBase_1.default {
    constructor(rootPath, bundleName) {
        super();
        this.jsonPath = path.join(rootPath, "messages", bundleName + ".json");
        try {
            let json = require(this.jsonPath);
            this.messages = Object.keys(json).reduce((acc, key) => {
                acc.set(key, String(json[key]));
                return acc;
            }, new Map());
        }
        catch (e) { }
    }
}
exports.default = AppJsonMessages;
//# sourceMappingURL=appJsonMessages.js.map