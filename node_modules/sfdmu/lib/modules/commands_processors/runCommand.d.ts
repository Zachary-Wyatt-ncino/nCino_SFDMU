import * as models from '../models';
import IPluginInfo from '../models/common_models/IPluginInfo';
import { ADDON_EVENTS } from '../components/common_components/enumerations';
import 'reflect-metadata';
import 'es6-shim';
import { Logger } from '../components/common_components/logger';
import { MigrationJob as Job } from '../models';
export declare class RunCommand {
    logger: Logger;
    pinfo: IPluginInfo;
    basePath: string;
    targetUsername: string;
    sourceUsername: string;
    apiVersion: string;
    canModify: string;
    script: models.Script;
    job: Job;
    exportJson: string;
    simulation: boolean;
    filePath: string;
    workingJson: any;
    useSf: boolean;
    logfullquery: boolean;
    constructor(pinfo: IPluginInfo, logger: Logger, basePath: string, sourceUsername: string, targetUsername: string, apiVersion: string, canModify: string, simulation: boolean, exportJson: string, useSf: boolean, logfullquery: boolean);
    loadAsync(): Promise<number>;
    setupObjectSetAsync(objectSetIndex: number): Promise<void>;
    createJobAsync(): Promise<void>;
    processCSVFilesAsync(): Promise<void>;
    prepareJobAsync(): Promise<void>;
    executeJobAsync(): Promise<void>;
    runAddonEventAsync(event: ADDON_EVENTS): Promise<void>;
    private _initScript;
    private _setupGlobalScriptParameters;
}
